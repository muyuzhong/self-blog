# 低温笔记 LowTemp Notes — 设计文档

> 版本:v1.0 / 2026.07.25
> 本文档是建站唯一设计依据。rule.md 是品牌 brief,本文档是落地规范。
> 凡本文档与 rule.md 冲突处,以本文档为准。

---

## 0. 设计论点

> **这个博客把"知识的成熟度"可视化为温度。**

绝大多数博客只有两种状态:已发布 / 草稿。低温笔记假设思想有物态:
尚在蒸腾的草稿、流动的探索、开始结晶的验证、完全结晶的稳定结论、以及升华消失的废弃观点。

整站只有一条公理:

> **越确定的思想,温度越低。**

由此推导出全部视觉决策:配色(冷)、动效(慢)、排版(结晶般的秩序)、以及四个签名元素。
惊艳感不来自装饰密度,而来自**这个隐喻被贯彻到每一个像素之后产生的整体感**。

保护阅读的底线只有一条:

> **删除测试** —— 任何一个视觉效果被移除后,页面必须依然成立、依然可读、依然有辨识度。
> 装饰是放大器,不是承重墙。

---

## 1. 核心系统:温度即状态

### 1.1 内容状态 → 温度映射

每篇文章、每个项目都带一个 `status`,映射为温度。温度是**认识论标签**,不是天气彩蛋。

| Status       | 温度      | 物态   | 含义                     |
| ------------ | --------- | ------ | ------------------------ |
| `Draft`      | 19.5°C    | 雾     | 尚在形成,可能随时改变    |
| `Exploring`  | 14.0°C    | 液态   | 有方向的探索,未定型      |
| `Tested`     | 8.4°C     | 结晶中 | 经过项目验证,趋于稳定    |
| `Stable`     | 4.2°C     | 结晶   | 相对成熟,可依赖          |
| `Deprecated` | —°C       | 升华   | 观点已改变,仅作存档      |

### 1.2 温度的三个显性出口

1. **文章页头状态栏**:`STATUS Stable · 4.2°C`,与 rule.md 的状态栏合并,不另设组件。
2. **阅读进度温度计**:文章页顶部 1px 进度线,从初温色(冷蓝 `#6E9FCB`)随阅读深度渐变到低温紫(`#8B82D9`),读完即"降至核心温度"。页面背景色同步做 ≤4% 的明度/饱和度插值——过程不可察觉,读完回头才意识到房间变冷了。实现:JS 只写一个 `--read-progress: 0..1`,颜色全部由 CSS `color-mix()` 完成。
3. **页脚站点温度**:`LOWTEMP SYSTEM · 11.6°C · QUIET · OPERATIONAL`。数值 = 全部公开内容温度的中位数,随发文自动变化,无后端。

### 1.3 404

绝对零度:`-273.15°C · SIGNAL LOST`,配一条停止呼吸的状态点。这是全站唯一允许"冷幽默"的地方。

---

## 2. 四个签名视觉元素

这四个元素构成"移除所有插图后依然有辨识度"的答案。全部低成本、可静态生成。

### 2.1 结晶生长(Generative Mark)

首页 hero 右侧的主视觉:**一棵由内容数据生成的结晶/羽枝结构**。

- 数据即图形:每篇文章 = 一条主枝,枝长 ∝ 字数,分枝角由分类决定(Engineering 偏右 15°、Research 偏左 15°、Build Logs 垂直、Reflections 随机 ±30°),枝端节点色温 = 文章温度。
- 生长动画:45 秒一个生长循环,1px 细线,低温紫/冷蓝,透明度 0.25–0.4,带 `vector-effect` 保持细线。
- `prefers-reduced-motion` 或 JS 失败时:渲染静态最终帧的 SVG,视觉依然成立。
- 它会随着发文真实生长——这是只属于这个博客的、无法被模板复制的视觉。

### 2.2 垂直冷轨(Cold Rail)

桌面端(≥1280px)页面最左边缘一条 48px 宽的固定竖轨:

```text
│
│  LTN-024 · ENGINEERING · 4.2°C        ← mono 10px,旋转 90°,弱化色
│
```

- 1px 竖线 + 90° 旋转的元信息,随页面变化显示当前文档编号。
- 成本极低,识别度极高——截图在任何地方出现都能认出是低温笔记。
- 窄屏自动隐藏,不设降级(内容不损失)。

### 2.3 Diff 语言

`+ / −` 作为微交互语法,只在四个位置出现,绝不滥用:

1. `::selection` 背景 = 低温青 18% 透明 —— 选中文本即"标记为增益"。
2. 文章内"核心观点"组件的引用行,行首带 mono `+` 前缀。
3. About 页一处真实的 diff 块(rule.md 已有:`- 等待一个绝对正确的方向 / + 在行动中修正方向`)。
4. 日志(Log)条目 hover 时,左侧出现 `+`,暗示"加入时间线"。

### 2.4 羽标(LT Mark)

Logo = 字母 `LT` 的竖笔延伸出 3–5 条平行斜线,同时读作:羽毛切面 / diff 行 / 结晶枝。

- 单文件 SVG,用于 favicon、导航左侧、页脚、结晶生长的根部。
- 不使用写实羽毛、不使用任何人物插画。

---

## 3. 色彩系统

沿用 rule.md 色板,补充功能性 token 与使用纪律。

### 3.1 深色(默认)

```css
--bg-0: #0B0D12;   /* 页面背景 */
--bg-1: #11141B;   /* 卡片、侧栏、代码区 */
--bg-2: #171B24;   /* 悬浮、选中、交互 */
--ink-0: #D8DCE6;  /* 主文字 */
--ink-1: #858B99;  /* 次级:日期、摘要 */
--ink-2: #555C6B;  /* 弱化:编号、注释 */
--accent: #8B82D9; /* 低温紫:唯一主强调,仅用于当前态/关键交互 */
--blue:   #6E9FCB; /* 冷蓝:链接、技术分类 */
--teal:   #70AAA6; /* 低温青:Stable、selection */
--mauve:  #77718F; /* 灰紫:标签 */
--red:    #C1787A; /* 警示:Deprecated、失败记录 */
--line:   #232833; /* 分割线(新增,替代用弱化文字色画线) */
```

### 3.2 浅色

| token    | 值        | 说明                     |
| -------- | --------- | ------------------------ |
| `--bg-0` | `#F2F1ED` | 实验记录纸               |
| `--bg-1` | `#E9E8E3` |                          |
| `--ink-0`| `#25272D` |                          |
| `--ink-1`| `#747780` |                          |
| `--accent`| `#655DAD`|                          |
| `--line` | `#D8D5CC` |                          |

### 3.3 纪律

- 任一视口截图,强调色面积 ≤ 15%,`<a>` 之外的交互元素才能用 `--accent`。
- 文字对比度:正文 ≥ 7:1(WCAG AAA),次级文字 ≥ 4.5:1。
- 不使用任何纯黑 `#000` 与纯白 `#FFF`。

---

## 4. 字体与排版:三重奏

排版的"惊艳"来自**三种字体气质的并置**:宋体的刊物感 × 无衬线的正文 × 等宽的实验室元信息。

| 角色     | 字体栈                                              | 用途                             |
| -------- | --------------------------------------------------- | -------------------------------- |
| 标题宋体 | `"Source Han Serif SC", "Noto Serif SC", serif`     | 文章标题、首页大字、章节题       |
| 正文无衬线 | `"Inter", "Source Han Sans SC", "PingFang SC", sans-serif` | 正文、UI                  |
| 等宽     | `"IBM Plex Mono", "SFMono-Regular", monospace`      | 编号、日期、状态栏、标签、代码   |

规范:

- 正文:17px / 行高 1.85 / 栏宽 680–720px(中文 32–38 字/行)。
- 文章 H1:宋体 34–40px / weight 600 / 行高 1.35。首页 hero "低温笔记" 可到 64–80px。
- 章节题 H2:宋体 24px;H3 起用无衬线 18px/600——长文深层级不需要刊物感。
- 元信息一律 mono 11–12px、全大写英文、字距 +0.08em:`LTN-024 · 2026.07.13 · 4.2°C`。
- 数字用 tabular figures(`font-feature-settings: "tnum"`),编号、日期、温度对齐才精密。
- 标题不用 700 以上粗重;段落间距(1.6em)> 行距,形成"段落呼吸"。
- 中西文混排:依赖浏览器自动间距,不手写空格。

---

## 5. 布局网格

桌面端三列非对称网格,是这个站"不像模板"的骨架:

```text
┌────┬──────────────────────────┬───────────┐
│ 48 │      内容列 680–720px     │  200px    │
│冷轨 │      (偏左,不居中)       │ 页边注列  │
└────┴──────────────────────────┴───────────┘
 固定      阅读的主权区域            ≥1280px 出现
```

- 内容列偏左而非居中——印刷刊物的页边注传统,也是冷轨存在的前提。
- **页边注列**(文章页,≥1280px):状态卡、目录、以及作者边注(frontmatter `notes:` 字段,手写感的短句,如"这里我后来后悔了")。
- <1280px:页边注列折叠为文末附录卡;冷轨隐藏;内容列居中。
- 全站最大宽度 1200px,垂直节奏基于 8px 基数,段落区块间距 64–96px。

---

## 6. 页面设计

### 6.1 首页

```text
[冷轨: LOWTEMP NOTES · EST.2026]

LOWTEMP NOTES ─────────────────────────  (mono kicker, 12px)
低温笔记                                  (宋体 72px,重 600)
在噪声退去之后,
记录仍然成立的东西。                      (宋体 24px,次级色)

AGENT ENGINEERING · CODING AGENT · RAG · SYSTEMS   (mono 12px)

[ 开始阅读 ]   [ 查看项目 ]               (幽灵按钮,见 7.1)

                    ┌─────────────┐
                    │  结晶生长    │  ← 右侧 40% 区域,SVG 生成式图形
                    │  (45s loop) │
                    └─────────────┘
```

向下依次(遵循 rule.md 第十三节结构):

1. **CURRENT RESEARCH** — 单卡,显示一个当前研究问题 + `STATUS / FOCUS / UPDATED` mono 状态行,左侧 2px 低温紫边。全站只此一处强调色大块出现。
2. **SELECTED WRITING** — 3–5 篇档案式列表(见 6.2)。
3. **PROJECTS** — 2–3 个 LAB 档案卡(见 6.4)。
4. **RECENT LOG** — 最近 5 条短记录,单行 mono 日期 + 一句话,无卡片。
5. **ABOUT FRAGMENT** — ≤100 字 + "More →"。
6. **Footer** — 羽标 / 导航 / `LOWTEMP SYSTEM · 11.6°C · QUIET · OPERATIONAL`。

### 6.2 文章列表(Writing)

顶部四个分类切换(不带路由,前端过滤):`ENGINEERING / RESEARCH NOTES / BUILD LOGS / REFLECTIONS`。

档案式列表项(rule.md 第六节,微调):

```text
LTN-021 · ENGINEERING · 4.2°C                        2026.07.18

Skill 的评价与优化                    ← 宋体 22px,绝对视觉中心
从提示词经验到可测试的工程资产          ← 副题,次级色

摘要最多两行。Skill 的价值不在于描述得多完整,而在于它能否
稳定改善 Agent 在真实任务中的表现。

AGENT · EVALUATION · SKILL            ← mono 标签,灰紫
────────────────────────────────────────────  1px --line
```

- 无封面图、无卡片底色,纯排版列表;hover 仅标题变 `--accent` + 行背景 2% 提亮。
- 温度随编号同行出现,颜色即温度:Stable 用低温青、Exploring 用冷蓝、Draft 用灰紫、Deprecated 用警示红且标题加删除线。

### 6.3 文章详情页

```text
[冷轨: LTN-024 · ENGINEERING · 4.2°C]   [顶部: 1px 阅读温度计]

LTN-024 · ENGINEERING                              2026.07.13

Skill 的评价与优化                        ← 宋体 36px
从提示词经验到可测试的工程资产

18 MIN READ · UPDATED 2026.07.20
────────────────────────────────────────
STATUS       Stable · 4.2°C
CONFIDENCE   High
SCOPE        Agent Engineering
────────────────────────────────────────

正文……                                  [页边注列: 目录 + 边注]
```

- 状态栏三行,真实存在于 frontmatter,不是装饰。
- 文末固定组件:`TEMPERATURE LOG` —— 本文温度变化史(`2026.05 Draft 19.5°C → 2026.07 Stable 4.2°C`),一行 mono,强化"知识会冷却"的隐喻。
- 上一篇/下一篇:只显示编号 + 标题,无卡片。

### 6.4 项目页(Projects)

实验档案式,遵循 rule.md 第八节:编号 `LAB-xxx`,`STATE / PROBLEM / HYPOTHESIS / SYSTEM / FAILED ATTEMPTS / NEXT` 分节,每节 mono 标签 + 正文。SYSTEM 用缩进树表示架构,不用流程图。

### 6.5 日志(Log)

最短形式:mono 日期 + 一句话 + 可选链接,按月份分组。不加卡片、不加标签。这是全站最"未完成的房间"的地方,允许随意。

### 6.6 关于(About)

三段:我是谁(≤150 字)/ 这个站是什么(温度系统的一段话说明)/ 一处 diff 块。不放技能条、不放时间轴、不放照片墙。

---

## 7. 组件库(文章内)

全部组件 = 1px 左边框(对应语义色)+ mono 小标签 + 正文,无背景色块、无圆角阴影。语义的"冷峻感"靠克制实现。

| 组件                 | 标签色 | 用途               |
| -------------------- | ------ | ------------------ |
| `CORE IDEA`          | 低温紫 | 核心观点,行首 `+` |
| `ENGINEERING DECISION` | 冷蓝 | 判断与取舍         |
| `FAILED ATTEMPT / n` | 警示红 | 失败记录,可编号   |
| `OPEN QUESTION`      | 灰紫   | 未解决问题         |
| `MARGIN NOTE`        | 弱化色 | 页边注(宽屏右列)  |

代码块:`--bg-1` 底色 + 1px `--line` 边框 + IBM Plex Mono 14px,左上角 mono 标注语言名。行内代码仅加底色,不加边框。

按钮(7.1):幽灵样式 —— 1px `--line` 边框、mono 12px 全大写,hover 时边框变为 `--accent` 并出现从左到右 240ms 的下划线,无填充变化。

---

## 8. 动效规范

关键词:**有生命迹象,但不打扰思考。**

| 场景           | 参数                                                     |
| -------------- | -------------------------------------------------------- |
| 页面切换       | View Transitions,200ms,内容淡入 + 上移 8px              |
| 通用进入       | 240ms,`cubic-bezier(0.22, 1, 0.36, 1)`,位移 ≤ 10px      |
| 链接下划线     | 从左向右生成,240ms                                       |
| 列表 hover     | 背景 +2% 亮度,不位移、不放大                             |
| 状态呼吸点     | 5s 一次,透明度 0.4↔1                                     |
| 结晶生长       | 45s 循环,枝线描边生长                                    |
| 阅读温度计     | 与滚动同步,无惯性动画                                    |

硬性禁止:视差、鼠标拖尾、粒子、逐字动画、毛玻璃、超过 300ms 的过渡、任何循环动画透明度 > 0.5。

无障碍:`@media (prefers-reduced-motion: reduce)` 下所有动画退化为静态帧/即时切换,温度渐变退化为纯色。这不是可选项。

---

## 9. 技术实现

- **框架**:Astro 5(静态输出、内容集合、内置 View Transitions)。不引入 React/Vue 运行时;结晶生长用原生 SVG + 少量 vanilla JS(< 3KB)。
- **样式**:原生 CSS + Custom Properties,单文件设计 token 层(`tokens.css`)+ 组件样式。不引 CSS 框架。
- **内容**:`src/content/` 下 Markdown/MDX,frontmatter:

```yaml
---
id: LTN-024
title: Skill 的评价与优化
subtitle: 从提示词经验到可测试的工程资产
category: engineering        # engineering | research | buildlog | reflection
status: stable               # draft | exploring | tested | stable | deprecated
temperature: 4.2             # 可选,默认由 status 映射
tags: [agent, evaluation, skill]
published: 2026-07-13
updated: 2026-07-20
excerpt: 摘要,两行以内。
tempLog:                     # 温度史,可选
  - { date: 2026-05-02, status: draft }
  - { date: 2026-07-13, status: stable }
notes:                       # 页边注,可选
  - { anchor: "评价对象", text: "这里我后来后悔了" }
---
```

- **编号**:LTN/LAB 编号手工维护进 frontmatter(可控、可乱序发布),不写自动化。
- **字体**:思源宋/黑 + IBM Plex Mono,本地子集化托管(中文按页面字符集子集),FOUT 用 `font-display: swap`。
- **性能预算**:首页 JS ≤ 50KB(gzip),LCP < 1.5s(4G),CLS = 0,Lighthouse 四项 100。任何让预算超标的视觉需求让路。
- **部署**:静态托管(Vercel / Cloudflare Pages 均可),无服务端依赖。

---

## 10. 验收标准

rule.md 五问之外,追加四条工程验收:

1. **删除测试**:逐一移除结晶生长、冷轨、温度渐变、呼吸点,页面依然成立、可读、可辨识。
2. **温度一致**:任意页面出现的温度数值,必须能从内容 frontmatter 推导;不允许手写死。
3. **reduced-motion 全通过**:系统开启减弱动效后,网站信息无损。
4. **对比度**:正文 AAA,次级 AA,用工具实测而非目测。

最终效果(沿用 rule.md 的表述,作为验收的感性标准):

> 打开网站时,第一感觉是冷静和秩序;开始阅读后,能感受到作者在认真思考;
> 读到结尾,才发现里面还保留着一点不明显的情绪。
