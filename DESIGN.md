# 暮羽中个人网站设计文档

## 1. 项目定位

**项目名称**：暮羽中个人网站  
**站点域名**：`https://muyuzhong.xyz`  
**当前部署形态**：Next.js 静态导出，面向 GitHub Pages 根域部署  
**核心用途**：个人品牌 + 项目作品集 + 技术博客 + 联系入口  
**目标受众**：技术面试官、潜在合作方、同行开发者、对 AI 工程和前端工程感兴趣的读者

这个站点的第一目标不是展示炫技特效，而是让访客快速判断三件事：

1. 这个人是谁，主要技术方向是什么。
2. 有哪些真实项目、文章和能力证据可以继续查看。
3. 如何用可靠方式联系到本人。

## 2. 当前设计方向

### 2.1 视觉气质

当前实现已经从早期的霓虹、3D 粒子、玻璃拟态方向，收敛为更克制的 **黑金编辑器风格**：

- 深色背景，但避免赛博霓虹和大面积发光。
- 单一金色强调色，用于编号、悬停、进度条和关键交互反馈。
- 方角、细线、括号标签、低饱和文字，形成代码编辑器和技术档案的感觉。
- 大号粗体标题负责视觉冲击，其他元素保持低干扰。

后续优化应沿用这个方向，不要重新引入大面积 cyan/purple 渐变、玻璃卡片、发光边框或装饰性 3D 背景。

### 2.2 品牌关键词

- 克制
- 工程感
- 可信
- 可核验
- 有个人技术判断

### 2.3 设计原则

- 作品和文章优先于装饰。
- 所有链接都必须可用，不能出现空壳入口。
- 联系方式必须真实，表单不能假装发送成功。
- 项目描述要突出本人贡献、技术难点和结果，而不是模板化项目名。
- 首页首屏要明确显示“暮羽中”这个个人品牌，而不是只放一句泛化口号。

## 3. 信息架构

当前首页结构：

```text
Home
├─ Hero：定位语、个人身份、滚动提示
├─ About：个人简介、关键统计、经历时间线
├─ Projects：精选项目卡片
├─ TechStack：技术能力列表
├─ Blog：最近技术文章
├─ Contact：联系说明、社交链接、邮件表单
└─ Footer：版权与返回顶部
```

独立页面：

```text
/blog/                博客归档页
/blog/[slug]/         MDX 文章详情页
/robots.txt           搜索引擎规则
/sitemap.xml          站点地图
```

## 4. 视觉系统

### 4.1 颜色

| Token | 当前用途 |
| --- | --- |
| `--background` | 页面主背景，接近黑色但保留暖色倾向 |
| `--foreground` | 主文字 |
| `--card` | 项目卡片、浮层、聊天窗口背景 |
| `--muted-foreground` | 次级说明、标签、时间、导航 |
| `--accent` | 金色强调，用于编号、hover、进度条和关键状态 |
| `--border` | 低对比细线分隔 |

颜色使用规则：

- 金色只用于强调，不用于铺满大面积背景。
- 交互反馈以颜色变化和细线为主，避免 glow。
- 浅色模式保留同一结构，只调整背景和文字对比。

### 4.2 字体

| 层级 | 字体 | 用途 |
| --- | --- | --- |
| Display | `Inter` / sans | Hero、章节标题 |
| Body | `Inter` / sans | 正文说明 |
| Label | `JetBrains Mono` | 括号标签、时间、导航、状态 |

注意：

- 不再把所有技术感都压到 monospace 上，正文保持可读性。
- 大标题可以强，但小标签要轻，避免页面噪声过高。

### 4.3 布局

- 页面最大宽度：`max-w-7xl`
- 横向内边距：移动端 `px-6`，桌面端 `lg:px-10`
- 章节垂直间距：`py-32` 到 `lg:py-40`
- 卡片半径：当前为方角，保持编辑器/档案感
- 纵向装饰字：仅桌面显示，作为低对比背景层

## 5. 组件规范

### 5.1 Navbar

目标：在不压迫导航的情况下，让“暮羽中”成为清晰的首屏品牌信号。

当前建议结构：

- 左侧品牌使用更突出的中文名。
- 可带一个方形字标，例如“暮”。
- 辅助小字说明方向，例如 `FULL STACK / AI TOOLS`。
- 导航项仍保持低调，避免和品牌争抢。

### 5.2 Hero

当前 Hero 使用大标题和一句身份说明。后续可继续优化：

- H1 可以更明确地包含个人方向或名字。
- 首屏应提供至少一个明确下一步：看项目、看博客、联系。
- 不建议恢复全屏 3D 背景，除非它能直接服务个人品牌，而不是单纯装饰。

### 5.3 Projects

项目卡片必须从“模板展示”升级为“证据展示”：

- 真实截图或界面缩略图。
- GitHub 和 Demo 分开显示。
- 写清本人负责的部分。
- 写清技术难点、解决方案和结果。
- 没有真实链接的项目不应伪装成已上线项目。

### 5.4 Blog

博客数据源必须和 `src/content/posts/*.mdx` 保持一致：

- `blogPosts` 中出现的 slug 必须有对应 MDX 文件。
- `/blog` 归档页必须存在。
- sitemap 只收录真实可访问文章。
- 列表不能展示会进入“文章内容加载失败”的文章。

### 5.5 Contact

静态站点没有后端时，不要做假提交。可选策略：

- 使用 `mailto:` 打开预填邮件。
- 接入 Formspree / EmailJS / Resend 等第三方服务。
- 直接展示邮箱和 GitHub，减少表单复杂度。

当前实现应至少做到：

- 不出现 `example.com` 占位邮箱。
- 点击提交后明确打开邮件客户端，由用户确认发送。
- toast 文案不能写“留言已发送”，除非确实经过后端发送成功。

### 5.6 ChatBot

当前聊天助手是本地 FAQ 匹配，不是真正 AI。后续有两种方向：

- 如果继续保留，命名应偏“快速导航”或“站点助手”。
- 如果要叫 AI 助手，需要接入真实问答能力，并基于本站内容回答。

## 6. 内容规范

个人站最忌模板感。所有内容都应尽量可核验：

- 邮箱、社交链接、GitHub 链接必须真实。
- 经历如不方便公开公司名，应明确用“某公司/某项目”但避免编造成熟履历。
- 项目数字要么有证据，要么降低为定性描述。
- 技术能力条不要滥用百分比，优先用“熟练领域 + 代表项目”证明。
- 博客文章要少而真，避免空壳标题。

## 7. 技术栈

| 类型 | 当前使用 |
| --- | --- |
| 框架 | Next.js App Router |
| 语言 | TypeScript |
| UI | React |
| 样式 | Tailwind CSS |
| 动画 | GSAP / Framer Motion |
| 图标 | Lucide React |
| 内容 | MDX |
| 导出 | Next.js static export |
| 部署 | GitHub Pages + `muyuzhong.xyz` |

## 8. 验收标准

每次发布前至少检查：

1. `npm run build` 成功。
2. `/blog` 存在且所有文章详情可访问。
3. `sitemap.xml` 不收录空壳文章。
4. 联系表单不会假成功。
5. 页面里没有 `example.com`、不存在的项目图片路径或无效主要链接。
6. GitHub Pages 根域部署时，构建产物不带 `/self-blog/` 子路径前缀。
