# 低温笔记 · 使用文档

> 这份文档回答三件事:怎么写、模板怎么渲染、温度怎么定。
> 原则一句话:**你只写 Markdown 和 frontmatter,其他全是自动的。**

---

## 一、快速开始

```bash
npm run dev      # 本地预览,改内容即时刷新 → http://localhost:4321
npm run build    # 构建静态文件到 dist/(部署的就是这个目录)
```

写作流程只有三步:

1. 在对应目录新建 `.md` 文件
2. 填 frontmatter(文件头部两条 `---` 之间的配置)
3. 写正文

---

## 二、写一篇新文章

### 1. 建文件

位置:`src/content/writing/`
文件名 = URL,建议格式 `编号-英文短名.md`:

```text
src/content/writing/ltn-025-agent-sandbox.md   →  /writing/ltn-025-agent-sandbox/
```

### 2. 编号

文章编号 `LTN-xxx`、项目编号 `LAB-xxx`,**手工维护**。看一眼现有最大编号,加一即可。
手工是有意为之:编号允许乱序发布(LTN-026 可以先于 LTN-025 发),它是档案号,不是序号。

### 3. frontmatter 全字段

```yaml
---
# —— 必填 ——
id: LTN-025                    # 档案编号
title: Agent Sandbox 的运行时边界
category: engineering          # engineering 工程 | research 研究笔记 | buildlog 建造日志 | reflection 随笔
status: exploring              # 状态,决定温度(见第四节)
published: 2026-07-25
excerpt: 两行以内的摘要,显示在文章列表。

# —— 建议填 ——
subtitle: 副题,一句话         # 显示在标题下方
tags: [sandbox, agent]         # 检索用,别超过 4 个
minutes: 12                    # 阅读时长(分钟)。还决定首页结晶枝的长度
confidence: 中                 # 高 | 中高 | 中 | 低
scope: Agent 基础设施          # 研究领域

# —— 选填 ——
updated: 2026-07-28            # 有实质修改时更新
featured: true                 # 上首页"精选文章"(建议保持 3—5 篇)
temperature: 13.5              # 手动覆盖温度,一般不用(见第四节)
tempLog:                       # 温度史,显示在文末。状态变化时追加一行
  - { date: 2026-07-25, status: draft }
  - { date: 2026-08-02, status: exploring }
notes:                         # 页边注,显示在文章右侧(宽屏)。写"后来才想到的话"
  - { anchor: "运行时边界", text: "这里其实和 LTN-022 是同一个坑。" }
---
```

### 4. 正文组件

正文就是 Markdown。四个语义组件用 HTML 写:

```html
<div class="note idea">
<div class="note-label">CORE IDEA</div>

核心观点。行首会自动出现 "+" 号。

</div>

<div class="note decision">
<div class="note-label">ENGINEERING DECISION</div>

工程判断:为什么这么取舍。

</div>

<div class="note failed">
<div class="note-label">FAILED ATTEMPT / 01</div>

失败记录,编号从 01 开始。

</div>

<div class="note open">
<div class="note-label">OPEN QUESTION</div>

没想清楚的问题。承认它比掩盖它更有价值。

</div>
```

diff 块(少量用):

```html
<div class="diff">
<div class="minus">- 旧做法</div>
<div class="plus">+ 新做法</div>
</div>
```

代码块、表格、引用用标准 Markdown 语法即可,样式都已配好。

---

## 三、模板是怎么渲染的

**是固定模板。** 三种内容类型各有一套,你写的每个字都会被自动装进对应模板:

| 内容类型 | 目录 | 模板自动给你的东西 |
| --- | --- | --- |
| 文章 | `src/content/writing/` | 页头(编号/分类/日期)、状态栏、阅读温度计、左侧冷轨、右侧目录+页边注、文末温度史、上下篇导航 |
| 项目 | `src/content/projects/` | 档案页头、问题/假设/系统/下一阶段分节、系统结构树 |
| 日志 | `src/content/log/` | 按月分组的时间线,一个文件就是一条 |

除了页面模板,这些东西也从你的 frontmatter **全自动推导**:

- 首页结晶图:每篇文章一条枝,枝长 ∝ `minutes`,角度随 `category`,颜色随温度
- 文章列表项:编号、分类、温度、日期、摘要、标签
- 页脚站点温度:全部内容温度的中位数
- 文章的冷轨文字:`LTN-025 · 工程 · 14.0°C`

**你不应该手写的**:温度数值(由状态映射)、"上一篇/下一篇"、目录、任何排版 HTML(除组件外)。

**想改模板本身**(比如文章页加一栏),才需要动代码:`src/pages/`(页面结构)、`src/components/`(部件)、`src/styles/`(样式)。写内容永远不需要碰它们。

---

## 四、温度系统怎么用

### 你选状态,温度自动来

```yaml
status: draft        # 19.5°C  雾      尚在形成,可能随时改
status: exploring    # 14.0°C  液态    有方向的探索,未定型
status: tested       #  8.4°C  结晶中  经过项目验证
status: stable       #  4.2°C  结晶    相对成熟,可依赖
status: deprecated   #   —°C   升华    观点已改变,仅存档
```

文章列表里,温度颜色随状态变化;`deprecated` 的标题会自动加删除线。

### 降温流程(建议的写作仪式)

```text
发新文      → status: draft 或 exploring,tempLog 记第一行
被项目验证  → 改 status: tested,tempLog 追加一行,更新 updated 日期
长期无反例  → 改 status: stable
观点变了    → 改 status: deprecated,不删文
```

文末的"温度史"就是这样一行行积累出来的——它是这篇文章的演化档案。

### 三个"不用管"

1. **单篇温度**不用填:由 `status` 映射。除非你有特殊理由(比如一篇 exploring 但你认为它格外冷),才用 `temperature: 数字` 覆盖。
2. **页脚站点温度**不用填:全部内容温度的中位数,自动算。
3. **阅读温度计**不用填:文章页顶部那条线,初温自动取文章温度。

### ⚠️ 一个重要约定

`status: draft` **不是"不发布"**,而是"以 19.5°C 的雾态公开发布"——构建后它会上线,标题、摘要都可见。这是设计意图:低温笔记鼓励发布未完成的思想。
真正不想公开的文件,就别放进 `src/content/`。

---

## 五、另外两类内容

### 日志(最短的内容)

`src/content/log/` 里一个文件一条,正文一句话:

```markdown
---
published: 2026-07-25
link: /writing/ltn-024-skill-evaluation/    # 可选
---

LTN-024 降到 4.2°C。三周没有反例,标记稳定。
```

### 项目

`src/content/projects/`,字段比文章特殊:

```yaml
---
id: LAB-004
title: 项目名
subtitle: 一句话说明
state: 开发中                # 自由文本:开发中 | 运行中 | 已搁置……
status: exploring            # 同样有温度
problem: 问题是什么。
hypothesis: 你的核心判断。
system:                      # 系统组成,渲染成树状
  - Context Layer
  - Minimal Diff
next: 下一步做什么。
order: 3                     # 项目页排序,小的在前
---

## 失败尝试          ← 正文自由发挥,会显示在档案下方
```

---

## 六、部署

`npm run build` 产物在 `dist/`,是纯静态文件,扔到任何静态托管(Vercel / Cloudflare Pages / GitHub Pages / OSS)即可。没有服务端依赖。

---

## 七、写作检查清单

发文前过一遍:

- [ ] 编号没和现有文章重复
- [ ] `status` 选的是真实状态,不是"希望它看起来的状态"
- [ ] 摘要两行以内,写的是判断不是广告
- [ ] `tempLog` 在状态变化时追加了
- [ ] 失败过的部分,有没有用 `FAILED ATTEMPT` 记下来
- [ ] `npm run build` 无报错

最后一条来自 rule.md,也是这个站的立场:

> 视觉是否安静到足以承载长文章?技术内容是否始终比装饰更突出?
> 你的写作也应该过同样的检验。
